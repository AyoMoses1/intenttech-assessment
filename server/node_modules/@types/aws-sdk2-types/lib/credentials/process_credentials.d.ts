import { HTTPOptions } from "aws-sdk2-types/lib/config-base";
import { Credentials } from "aws-sdk2-types/lib/credentials";
export class ProcessCredentials extends Credentials {
    /**
     * Creates a new ProcessCredentials object.
     */
    constructor(options?: ProcessCredentialsOptions);
}

interface ProcessCredentialsOptions {
    profile?: string;
    filename?: string;
    httpOptions?: HTTPOptions;
}
